<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent"/>
    <stroke android:width="2.2dp" android:color="@color/checkbox_color"/> <!-- Màu viền -->
</shape>
