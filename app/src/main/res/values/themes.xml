<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ApartmentManagement" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:windowBackground">@color/white</item>

        <!-- Màu chủ đạo của app -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="textInputStyle">@style/AppTextInputLayoutStyle</item>
        <item name="editTextStyle">@style/AppEditTextStyle</item>
        <item name="fontFamily">@font/inter</item>
        <item name="android:textColor">@color/text_primary</item>"

    </style>

    <style name="Theme.ApartmentManagement" parent="Base.Theme.ApartmentManagement" />
</resources>